{
  "name": "sand-art",
  "version": "0.0.10",
  "private": true,
  "type": "module",
  "description": "Sand Art — a jar of coloured sand on the arcade's compiled sim-sand kernel.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node tools/verify-artifact.mjs && node --test 'tests/*.test.js'",
    "stage": "node tools/stage.mjs dist"
  }
}
